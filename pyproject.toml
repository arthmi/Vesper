# pyproject.toml
[project]
name = "vesper"
version = "0.1.0"
description = "A minimal, keyboard-first Hyprland shell focused on productivity"
require-python = ">3.11"
dependencies = [
    "fabric",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tonshell"]
